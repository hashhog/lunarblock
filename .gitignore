*.rock
*.swp
*.o
*.so
.env
luarocks/
lib/librocksdb*
lib/libsecp256k1*

.claude/skills/

banned.dat
anchors.dat

# Project-meta audit artifacts live in the meta-repo (audit-archive/).
audit/
CORE-PARITY-AUDIT/

# Runtime files the node writes into its cwd (the test runner starts a node
# whose cwd is the repo). Never commit these.
peers.dat
banlist.json
mempool.dat
fee_estimates.json
*.log
